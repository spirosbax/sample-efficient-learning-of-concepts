# external/BISCUIT/setup.py
from setuptools import setup, find_packages

setup(
    name="sample_efficient_learning_of_concepts",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add dependencies as needed
    ],
)